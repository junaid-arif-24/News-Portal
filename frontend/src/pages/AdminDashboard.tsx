import React from 'react';

const AdminDashboard: React.FC = () => {
  return (
    <div>
      <h1>Admin Dashboard</h1>
      {/* Add components for managing categories, news, etc. */}
    </div>
  );
};

export default AdminDashboard;
